@model IEnumerable<NauAnUtLanh.Database.Food>

<div class="container">
    <div class="title cnt-title">
        <h3>Món ăn ưa thích</h3>
    </div>
    <div class="row">
        @{
            foreach (var food in Model)
            {
                <div class="col-lg-3 col-md-3 col-sm-6 col-xs-6">
                    @*<div class="my-padding">
                        <img src="@Url.Content($"~/upload/food/{food.Avatar}")" alt="" title="@food.FoodName" class="img-responsive thumbnail"/>
                        <div class="col-lg-3 col-md-3 col-sm-12 col-xs-12 food-name">@food.FoodName</div>
                    </div>*@
                    <div class="thumbnail">
                        <a href="@Url.Content($"~/upload/food/{food.Avatar}")">
                            <img src="@Url.Content($"~/upload/food/{food.Avatar}")" alt="Lights" style="width: 100%; height: 100%" class="img-responsive">
                            <div class="caption">
                                <p>@food.FoodName</p>
                            </div>
                        </a>
                    </div>
                </div>
            }
        }
    </div>
    <div class="clearfix"></div>
    <br />
    <div class="text-center">
        @Html.ActionLink("Xem thêm", "index", "food", new { id = "loadMore" })
    </div>
</div>

